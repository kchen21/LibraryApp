<div class="jumbotron text-center">
  <h1>Coming Soon</h1>

  <br/><br/>

  <p>Don't miss our launch date! Request an invitation now!</p>

  {{#if responseMessage}}
    <div class="alert alert-success">Thank you! We haved saved your email address with the following id: {{model.id}}</div>

  {{else}}

    <div class="form-horizontal form-group form-group-lg row has-feedback {{if model.isValid 'has-success'}}">
      <div class="col-xs-10 col-xs-offset-1 col-sm-6 col-sm-offset-1 col-md-5 col-md-offset-2">
        {{input type="email" value=model.email class="form-control" placeholder="Please enter your email address." autofocus="autofocus"}}
        {{#if model.isValid}}
          <span class="glyphicon glyphicon-ok form-control-feedback" aria-hidden="true" style="padding-right:15px;"></span>
        {{/if}}
      </div>
      <div class="col-xs-10 col-xs-offset-1 col-sm-offset-0 col-sm-4 col-md-3">
        <button class="btn btn-primary btn-lg btn-block" disabled={{model.isDisabled}} {{action 'saveInvitation' model}}>Request invitation</button>
      </div>
    </div>

  {{/if}}

  <br/><br/>
</div>
